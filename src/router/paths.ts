export enum paths {
  home = "/",
  register = "/register",
  login = "/login",
  availableTimes = "/available-items",
  reservedTimes = "/reserved-items",
  adminPage = "/admin",
  users = "/users",
}
